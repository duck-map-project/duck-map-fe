import styled from 'styled-components';

import {
  ListItem,
  ItemText,
  ButtonWrapper,
  CommonButton,
} from '../Category/CategoryListItemStyle';

export const TypeListItem = styled(ListItem)``;

export const TypeText = styled(ItemText)``;

export const TypeButtonWrapper = styled(ButtonWrapper)``;

export const TypeCommonButton = styled(CommonButton)``;
